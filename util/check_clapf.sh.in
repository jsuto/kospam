#!/bin/sh
##
## check_clapf.sh, 2006.11.16, SJ
##
## periodicaly check clapf-related services and restart them if necessary

NUM=`ps uaxw | grep clapf | grep -c ^USER`

if [ $NUM -eq 0 ]; then su USER -c 'export TMPDIR=YOUR_QUEUE; YOUR_PATH/clapf 2> /dev/null &' ; fi

